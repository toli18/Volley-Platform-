# No external dependencies are required for the static wireframe or mock API.
# This file is present to satisfy platforms that expect a requirements list
# during deployment (e.g., Render, Netlify functions, Railway).
